<p style="color: green"><%= notice %></p>

<%= render @event %>

<div>
  <%= link_to "Edit this event", edit_event_path(@event) %> |
  <%= link_to "Back to Events", lounge_events_path(@event.lounge) %> |

  <%= button_to "Destroy this event", @event, method: :delete %>
</div>
